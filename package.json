{
  "name": "deepads-client",
  "version": "1.0.0",
  "description": "DeepAds client",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Francisco Roca",
  "license": "ISC",

  "dependencies": {
    "@types/node-fetch": "^2.3.7",
    "node-fetch": "^2.6.0",
    "socket.io": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^12.0.10",
    "@types/socket.io": "^2.1.2",
    "tslint": "^5.18.0",
    "typescript": "^3.5.2"
  }
}
